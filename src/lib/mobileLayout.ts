/* Mobile counterpart to layout.ts, dimensions per the Figma mobile frames
   (node 539:187 and siblings) — a fixed 393x852 design surface, scaled to fit like
   the desktop stage, kept separate so desktop's constants are never touched. */
export const MOBILE_FRAME_W = 393;
export const MOBILE_FRAME_H = 852;

export const MOBILE_HERO_CARD_W = 336;
export const MOBILE_HERO_CARD_H = 336;

export const MOBILE_CASE_CARD_W = 336;
export const MOBILE_CASE_CARD_H = 527;
